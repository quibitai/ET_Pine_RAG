{
  "version": 2,
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm install tailwindcss@^3.4.1 postcss@^8 @tailwindcss/typography@^0.5.15 && pnpm build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "regions": ["sfo1"],
  "functions": {
    "app/api/**/*": {
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
} 